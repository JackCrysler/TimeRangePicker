{
  "name": "vue-grid-picker",
  "version": "1.0.6",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jack_crylser@163.com",
  "license": "ISC",
  "description": "A grid time picker ui component for vuejs. Only support hour picker currently.",
  "keywords": [
    "time picker",
    "grid picker",
    "range picker",
    "disabled hour ranges",
    "time select",
    "vue ui component"
  ],
  "peerDependencies": {
    "vue": "^3.0.0"
  }
}
