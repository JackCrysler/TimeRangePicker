{
  "name": "vue-grid-picker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "A grid picker component for Vue.js, only for hour picker, minute picker, and time picker. You can set disabled ranges that users cannot select."
}
